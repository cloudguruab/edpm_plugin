plugin: edpm_plugin                    # Name of the plugin
path_to_inventory: /home/adrbrown/redhat/ctrl-plane/edpm_plugin  # Directory location of inventory
inventory_file: edpm_inventory.yaml    # Name of the inventory file 
